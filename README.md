# Node Wordpress Template

# Instalación

Ejecutar desde la carpeta principal ```sudo bin/install.sh```

Nota: Eliminar el archivo index.html de la carpeta /var/www o /var/www/html

# Uso

Ejecutar desde la carpeta principal ```gulp```

